name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Installing packages
      run: npm i
    - name: Build and run tests
      run: |
        nvm use v10
        npm run build
        npm run test
